# Knowledge Capture Examples

This folder is reserved for synthetic examples of human-approved knowledge capture records. Do not store real user/private content here.

v3.5 uses runtime JSONL paths under `data/knowledge_capture/`, but tests should use temporary directories and documentation examples should remain synthetic.
