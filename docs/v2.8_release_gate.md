# v2.8 Release Gate

## Summary

The v2.8 release gate validates the current demo-ready state after Lazy RAG startup work, language-aware output, RAG prompt policy updates, HTTP/2 safe synthetic demo support, screenshot refresh, and public documentation cleanup.

This gate confirms that the project remains inside its intended safety boundary.

## Branch and Scope

- Branch validated: `v2.8-lazy-rag-startup`
- Release gate status: PASSED
- Environment: Windows + project virtual environment

v2.8 includes:

- Lazy RAG startup loading.
- Lightweight output-language policy.
- Language-aware AI Analyst Brief, Evidence Gap Analyzer, and RAG prompt context.
- Removal of hardcoded Traditional-Chinese-only RAG prompt behavior.
- HTTP/2 Resource Exhaustion safe synthetic demo scenario.
- HTTP/2 launcher preview polish.
- Demo scenario loading clears stale active context.
- Updated screenshot set for the current Streamlit SOC Console.

## Validation Results

| Check | Result |
|---|---|
| `python -m pytest` | PASSED: `1178 passed` |
| `python -m ruff check .` | PASSED |
| `python -m mypy app.py modules tests` | PASSED: `Success: no issues found in 164 source files` |
| `git diff --check` | PASSED |
| `gitleaks detect --source . --verbose --redact --gitleaks-ignore-path .gitleaksignore` | PASSED |
| Screenshot refresh | PASSED |
| Documentation refresh | PASSED |

## Gitleaks Note

A historical false positive was detected by the default gitleaks scan in `modules/rag_qa.py` safety-boundary text. The matched text was not a real secret. It was a defensive wording phrase about simulated/non-operational actions.

The false positive is documented in `.gitleaksignore`, and the release-gate scan was rerun with:

```powershell
gitleaks detect --source . --verbose --redact --gitleaks-ignore-path .gitleaksignore
```

Result: no leaks found.

## Screenshot Evidence

The current English screenshot set under `docs/screenshots/en/` was refreshed for the v2.8 demo path (Traditional Chinese UI screenshots are preserved under `docs/screenshots/zh-TW/`):

- `01_console_home.png`
- `02_fast_command_injection_analysis.png`
- `03_ai_analyst_brief.png`
- `04_evidence_gap_analyzer.png`
- `05_knowledge_qa_rag.png`
- `06_similar_cases.png`
- `07_relationship_graph.png`
- `08_case_draft_export.png`
- `09_http2_resource_exhaustion_demo.png`
- `10_full_ai_assisted_optional.png`

The HTTP/2 Resource Exhaustion demo screenshot confirms:

- The launcher card uses a compact summary preview.
- The full synthetic input remains available in the textarea.
- Loading the HTTP/2 scenario clears stale Command Injection active context.
- The demo remains synthetic, advisory, and non-operational.

## Safety Boundary Confirmation

Confirmed unchanged:

- Rule-Based Detector is the detection authority.
- Risk Level and Decision are deterministic.
- `BLOCK`, `MONITOR`, and `ALLOW` are simulated.
- RAG, LLM, AI Analyst Brief, and Evidence Gap provide advisory context only.
- No real firewall, WAF, EDR, account, cloud, SIEM, or SOAR action is performed.
- No exploit, proof-of-concept, or traffic generation is provided.
- Human review is required.

## Conclusion

v2.8 release gate passed. The branch is ready for merge/tag review after final git status is clean.
